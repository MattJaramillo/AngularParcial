export class Plantas {
    numero: number;
    nombre: string;
    tipo: string;
    clima: string;

    public constructor(numero: number, nombre: string, tipo: string, clima: string){
        this.numero = numero;
        this.nombre = nombre;
        this.tipo = tipo;
        this.clima = clima;
    }
}
